# LlavaCode
Llava variation for unified code + AST representations


Codebleu package may have some issues with installation. `pip install codebleu`, then `pip install tree-sitter==0.24.0 tree-sitter-python==0.23.4`. Codebleu may throw a warning about compatibility, but it should work, see `https://github.com/k4black/codebleu/issues/62`.